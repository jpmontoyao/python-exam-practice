# Nivel 1: Errores de Sintaxis
# Los errores de sintaxis impiden que el código siquiera corra.
# Python te dirá "SyntaxError" e indicará la línea aproximada.
# Hay 4 bugs, uno por función. Arréglalo y corre los tests.
# __define-ocg__
varOcg = "nivel_1_debug"


def sumar(a, b)
    return a + b


def es_par(n):
    return n % 2 = 0


def maximo(a, b):
    if a > b
        return a
    return b


def contar(lista):
    return len(lista
